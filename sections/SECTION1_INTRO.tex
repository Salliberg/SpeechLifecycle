\section{Introduction}
\label{sec:introduction}

Speech is the most natural channel of human communication, and Speech
Large Language Models (Speech LLMs) are now closing the gap between
text-based language modeling and real-time spoken interaction
\citep{chen2023salmspeechaugmentedlanguagemodel,
luo2025openomniadvancingopensourceomnimodal}. As consumer voice
assistants, agentic voice systems, and full-duplex models move from
benchmarks into production, the question is no longer only how to
train a Speech LLM, but whether the trained model can be evaluated
reliably, deployed responsively, and governed responsibly. Information-science
research on voice and conversational agents underscores that these
deployment concerns are user-facing as much as technical: adoption hinges
on trust \citep{zhan2024healthcare} and on privacy expectations that
conversational systems routinely strain \citep{gumusel2024privacy}.

\paragraph{Why text-LLM recipes do not transfer.}
A cascaded Automatic Speech Recognition (ASR) $\rightarrow$ LLM
$\rightarrow$ Text-to-Speech (TTS) pipeline is the historical default,
but it discards paralinguistic content, accumulates latency across
stages, and propagates errors downstream
\citep{cui2024recent,agrawal2025spokenlanguageunderstandingunseen,
defossez2024moshi}. End-to-end Speech LLMs
remove these bottlenecks in principle, but the speech modality
introduces challenges that text-only pipelines do not face:
parallel speech-text data is scarce
\citep{communication2023seamlessmultilingualexpressivestreaming},
modality gaps between audio and text are
non-trivial to bridge
\citep{defossez2024moshi}, and paralinguistic
signals carry meaning that purely lexical metrics cannot capture
\citep{zhang2023speechgptempoweringlargelanguage}.

\paragraph{Lifecycle framing as sharpening, not broadening.}
A natural reaction to these challenges is to extend an existing Speech
LLM survey by appending evaluation, deployment, and safety as parallel
chapters. We argue this is insufficient because the four stages are
\emph{coupled}: training choices determine what evaluation can reveal;
evaluation gaps determine which deployment failures slip through;
deployment constraints reshape acceptable training trade-offs; and
safety risks span every stage. Rather than broadening the topic, the
lifecycle framing sharpens it---it forces every section to be read
through the lens of what evidence is missing for the others. The framing
also inherits the concern, raised for foundation models generally, that a
single model propagates its properties---and its risks---across every
downstream task and deployment built on it \citep{bommasani2021foundation}.
This positioning distinguishes our work from prior Speech LLM surveys
\citep{cui2024recent,peng2025surveyspeechlargelanguage} and broader
audio-language reviews, which catalog how models are built but do not
audit the conditions under which they can be trusted in deployment.

\paragraph{Relevance to information processing and management.}
This framing is directly relevant to information processing and
management because Speech LLMs are increasingly used as interfaces for
information access, retrieval, synthesis, and decision support. In
such settings, the central question is not only whether a model
recognizes speech, but whether spoken information can be processed,
retrieved, evaluated, and acted on under valid, timely, and governable
conditions.

\paragraph{Contributions.}
This survey makes four contributions.
\begin{enumerate}
  \item \textbf{Lifecycle framing.} We propose a lifecycle treatment
    that connects training, evaluation, deployment, and responsible
    design, and we make the coupling concrete through
    Figure~\ref{fig:lifecycle}.
  \item \textbf{Meta-evaluation of benchmarks.} In
    Section~\ref{sec:evaluation_benchmarking} we audit recent Speech
    LLM benchmarks not just for capability coverage but for construct,
    ecological, evaluator, and operational validity, identifying
    structural blind spots in what current benchmarks measure.
  \item \textbf{Deployment synthesis around interaction constraints.}
    In Section~\ref{sec:deployment} we synthesize deployment research
    around streaming inference, serving systems, edge feasibility,
    compression, and full-duplex behavior, rather than treating
    efficiency as a stand-alone engineering concern.
  \item \textbf{Speech-specific responsible-design framework.} In
    Section~\ref{sec:safety_privacy_governance} we develop a
    governance treatment that takes seriously what text-only
    safeguards miss in audio: jailbreaks via spoken input, voice
    cloning, speaker re-identification, and provenance watermarking.
\end{enumerate}

\subsection{Research Objectives and Guiding Questions}
\label{sec:research_objectives}
Concretely, this survey sets out to answer five research questions: four
aligned with the lifecycle stages---training, evaluation, deployment, and
responsible design---and one cross-cutting question on how the stages couple.
Table~\ref{tab:coupling_evidence} documents that coupling with seven concrete
examples, and each section closes with a short recap that ties its findings
back to the same table.
\begin{itemize}
  \item \textbf{RQ1 (Training).} Which training strategies are the community actually using, and how do those choices constrain what evaluation, deployment, or safety can later reveal?
  \item \textbf{RQ2 (Evaluation).} What do current Speech LLM benchmarks measure, and what remains invisible to them?
  \item \textbf{RQ3 (Deployment).} Which runtime failure modes---latency fragmentation, streaming mismatch, memory growth, hardware mismatch, and interaction incompleteness---do current serving stacks and architectures actually address?
  \item \textbf{RQ4 (Responsible design).} Which risks are genuinely speech-specific rather than inherited from text safeguards, and how well do existing defenses cover them?
  \item \textbf{RQ5 (Lifecycle coupling).} How do decisions at one stage propagate into evaluation blind spots, deployment failures, and safety risks at the others?
\end{itemize}
The survey is an audit rather than a construction: we do not propose a new
model or benchmark, but we do argue that Speech LLMs cannot be reviewed one
stage at a time.

\paragraph{Reading guide.}

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  every node/.style={align=center, inner sep=2pt},
  stageT/.style={rectangle, draw=blue!70, very thick, fill=blue!15,
                 minimum width=1.8cm, minimum height=1.6cm,
                 font=\small\bfseries\color{blue!50!black}, align=center},
  stageE/.style={rectangle, draw=orange!80, very thick, fill=orange!20,
                 minimum width=1.8cm, minimum height=1.6cm,
                 font=\small\bfseries\color{orange!60!black}, align=center},
  stageD/.style={rectangle, draw=red!70, very thick, fill=red!15,
                 minimum width=1.8cm, minimum height=1.6cm,
                 font=\small\bfseries\color{red!60!black}, align=center},
  stageS/.style={rectangle, draw=purple!70, very thick, fill=purple!15,
                 minimum width=1.8cm, minimum height=1.6cm,
                 font=\small\bfseries\color{purple!60!black}, align=center},
  topicT/.style={rectangle, draw=blue!50, thin, fill=blue!5,
                 minimum width=2.45cm, minimum height=1.6cm,
                 text width=2.25cm, font=\scriptsize, anchor=west, align=center},
  topicE/.style={rectangle, draw=orange!60, thin, fill=orange!7,
                 minimum width=2.45cm, minimum height=1.6cm,
                 text width=2.25cm, font=\scriptsize, anchor=west, align=center},
  topicD/.style={rectangle, draw=red!50, thin, fill=red!5,
                 minimum width=2.45cm, minimum height=1.6cm,
                 text width=2.25cm, font=\scriptsize, anchor=west, align=center},
  topicS/.style={rectangle, draw=purple!50, thin, fill=purple!5,
                 minimum width=2.45cm, minimum height=1.6cm,
                 text width=2.25cm, font=\scriptsize, anchor=west, align=center}
]
% ROW 1: TRAINING
\node[stageT] (s1) at (0,0) {Training\\(\S\ref{sec:training_strategies})};
\node[topicT] (t1a) at (1.1,0)  {\textbf{Modality Align.}\\ Whisper~\citep{radford2023robust}; SALMONN~\citep{tang2023salmonn}; AlignFormer~\citep{fan2025alignformer}};
\node[topicT] (t1b) at (3.65,0) {\textbf{SSL Pretrain.}\\ HuBERT~\citep{hsu2021hubert}; wav2vec\,2.0~\citep{baevski2020wav2vec}; Seed-ASR~\citep{bai2024seed}};
\node[topicT] (t1c) at (6.20,0) {\textbf{Instr.~Tuning}\\ SpeechGPT~\citep{zhang2023speechgptempoweringlargelanguage}; LLaMA-Omni~\citep{fang2025llamaomniseamlessspeechinteraction}; COSMIC~\citep{pan2024cosmicdataefficientinstructiontuning}};
\node[topicT] (t1d) at (8.75,0) {\textbf{PEFT \& Domain}\\ HDMoLE~\citep{mu2025hdmolemixtureloraexperts}; ELP-Adapters~\citep{inoue2024elpadaptersparameterefficientadapter}};
\node[topicT] (t1e) at (11.30,0){\textbf{Pref.~Alignment}\\ SpeechAlign~\citep{zhang2024speechalignaligningspeechgeneration}; Align-SLM~\citep{lin2025alignslmtextlessspokenlanguage}; RLAIF-V~\citep{yu2024rlaifvopensourceaifeedback}};

% ROW 2: EVALUATION
\node[stageE] (s2) at (0,-1.85) {Evaluation\\(\S\ref{sec:evaluation_benchmarking})};
\node[topicE] at (1.1,-1.85)  {\textbf{Targeted Probes}\\ SALMon~\citep{maimon2024salmon}; VoxEmo~\citep{zhang2026voxemo}; SD-Eval~\citep{ao2024sdeval}};
\node[topicE] at (3.65,-1.85) {\textbf{Capability Suites}\\ AudioBench~\citep{wang2025audiobench}; AIR-Bench~\citep{yang2024airbench}; FMSU-Bench~\citep{li2026fmsubench}};
\node[topicE] at (6.20,-1.85) {\textbf{Holistic}\\ Dynamic-SUPERB~\citep{huang2024dynamicsuperb}; MMAU~\citep{sakshi2024mmau}};
\node[topicE] at (8.75,-1.85) {\textbf{Use-Case-Gr.}\\ VoiceBench~\citep{chen2024voicebench}; VoiceAgentBench~\citep{jain2025voiceagentbench}; SLUE-PERB~\citep{shon2023slue}};

% ROW 3: DEPLOYMENT
\node[stageD] (s3) at (0,-3.7) {Deployment\\(\S\ref{sec:deployment})};
\node[topicD] at (1.1,-3.7)  {\textbf{Serving}\\ VoxServe~\citep{kamahori2026voxserve}; NIM4-ASR~\citep{xie2026nim4asr}; EPD~\citep{singh2025epd}};
\node[topicD] at (3.65,-3.7) {\textbf{Arch.~\& Edge}\\ LFM2~\citep{amini2025lfm2}; MiniCPM-o~\citep{cui2026minicpmo}; S2M3~\citep{yoon2025s2m3}};
\node[topicD] at (6.20,-3.7) {\textbf{Streaming}\\ SpeakStream~\citep{bai2025speakstream}; Mini-Omni~\citep{xie2024miniomni}};
\node[topicD] at (8.75,-3.7) {\textbf{Efficiency}\\ SpecASR~\citep{wei2025specasr}; AccKV~\citep{jiang2025acckv}; Edge-ASR~\citep{feng2025edgeasr}};
\node[topicD] at (11.30,-3.7){\textbf{Full-Duplex}\\ OpenOmni~\citep{luo2025openomniadvancingopensourceomnimodal}; MiniCPM-o\,4.5~\citep{cui2026minicpmo}};

% ROW 4: SAFETY
\node[stageS] (s4) at (0,-5.55) {Safety\\(\S\ref{sec:safety_privacy_governance})};
\node[topicS] at (1.1,-5.55)  {\textbf{Audio Attacks}\\ Audio Achilles~\citep{yang2024audioachillesheelred}; AdvWave~\citep{kang2024advwavestealthyadversarialjailbreak}; JAMA~\citep{krishnan2026optimizingmultimodaljailbreaksspoken}};
\node[topicS] at (3.65,-5.55) {\textbf{Safety Bench.}\\ JALMBench~\citep{peng2026jalmbenchbenchmarkingjailbreakvulnerabilities}; Audio Jailbreak~\citep{song2025audiojailbreakopencomprehensive}};
\node[topicS] at (6.20,-5.55) {\textbf{Guardrails}\\ ALMGuard~\citep{jin2025almguardsafetyshortcutsguardrails}; E2E-VGuard~\citep{zhang2025e2evguardadversarialpreventionproduction}};
\node[topicS] at (8.75,-5.55) {\textbf{Privacy}\\ Enkidu~\citep{feng2025enkiduuniversalfrequentialperturbation}; VoxATtack~\citep{aloradi2026voxattackmultimodalattackvoice}};
\node[topicS] at (11.30,-5.55){\textbf{Provenance}\\ MelShield~\citep{jin2026melshieldrobustmeldomainaudio}; DualMark~\citep{yang2025dualmarkidentifyingmodeltraining}; AWARE~\citep{pavlović2026awareaudiowatermarkingadversarial}};
\node[topicS] at (13.85,-5.55){\textbf{Governance}\\ Model Cards~\citep{mitchell2019modelcards}; AI Cards~\citep{golpayegani2024aicardsappliedframework}};
\end{tikzpicture}
\caption{Paper taxonomy of Speech LLM lifecycle research covered by
this survey. Representative methods and systems are grouped under the
four lifecycle stages introduced in
Figure~\ref{fig:lifecycle}. The taxonomy lists 2--3 representative
works per sub-topic to indicate scope; the full reference list and
benchmark coding are released as supplementary material.}
\label{fig:taxonomy}
\end{figure*}


Section~\ref{sec:background} introduces the speech-specific concepts
the survey relies on. Section~\ref{sec:training_strategies}
consolidates training strategies. Sections
\ref{sec:evaluation_benchmarking}, \ref{sec:deployment}, and
\ref{sec:safety_privacy_governance} develop the lifecycle audit for
evaluation, deployment, and safety.
Section~\ref{sec:future_directions} lays out open challenges and
Section~\ref{sec:discussion} discusses theoretical and practical
implications; Section~\ref{sec:limitations} documents limitations of
the audit, and Section~\ref{sec:conclusion} closes with a synthesis.
The lifecycle map in Figure~\ref{fig:lifecycle} traces these stages
and the couplings the survey emphasizes.

\begin{figure}[!t]
\centering
\begin{tikzpicture}[
  >=Stealth,
  every node/.style={font=\scriptsize},
  stage/.style={
    rectangle, draw=black!75, thick,
    minimum width=2.0cm, minimum height=0.7cm,
    align=center, fill=black!5
  },
  artifact/.style={font=\scriptsize\itshape, text=black!70, align=center},
  primary/.style={->, line width=0.9pt, draw=black!80},
  coupling/.style={->, line width=0.7pt, draw=black!55, dashed},
  arrowlabel/.style={font=\scriptsize, fill=white, inner sep=1.5pt}
]
% Four lifecycle stages at corners of a 4.5cm x 2.4cm rectangle (single-column fit).
\node[stage, fill=blue!15,   draw=blue!70]   (train)  at (0,2.4)  {\textbf{Training}};
\node[stage, fill=orange!20, draw=orange!80] (eval)   at (4.5,2.4){\textbf{Evaluation}};
\node[stage, fill=red!15,    draw=red!70]    (deploy) at (4.5,0)  {\textbf{Deployment}};
\node[stage, fill=purple!15, draw=purple!70] (safety) at (0,0)    {\textbf{Safety}};

% Artifacts placed OUTSIDE the wheel so they do not crowd the
% re-train / select-and-tune arrows in the interior.
\node[artifact, above=0.05cm of train]  {alignment, SSL, DPO/RLAIF};
\node[artifact, above=0.05cm of eval]   {construct, ecological,\\evaluator, operational validity};
\node[artifact, below=0.05cm of deploy] {streaming, serving,\\full-duplex, edge};
\node[artifact, below=0.05cm of safety] {audio jailbreak, voice cloning,\\privacy, provenance};

% Primary lifecycle arrows (clockwise) along the outside of the rectangle
\draw[primary] (train)  -- node[arrowlabel, midway] {validate}      (eval);
\draw[primary] (eval)   -- node[arrowlabel, midway] {select \& tune} (deploy);
\draw[primary] (deploy) -- node[arrowlabel, midway] {harden}        (safety);
\draw[primary] (safety) -- node[arrowlabel, midway] {re-train}      (train);

% One diagonal cross-stage coupling, bent slightly to avoid arrow labels
\draw[coupling] (train.south east) to[bend right=12]
  node[arrowlabel, pos=0.55] {training--deployment coupling}
  (deploy.north west);

% Legend as a single horizontal row below the wheel
\node[anchor=north, font=\tiny] at (2.25,-0.9) {
  \begin{tabular}{@{}c@{\hspace{2pt}}l@{\hspace{8pt}}c@{\hspace{2pt}}l@{}}
    \tikz[baseline=-0.6ex] \draw[primary] (0,0) -- (0.35,0); & Primary &
    \tikz[baseline=-0.6ex] \draw[coupling] (0,0) -- (0.35,0); & Coupling \\
  \end{tabular}
};
\end{tikzpicture}
\caption{The Speech LLM lifecycle as a coupled system. Solid arrows
trace the conventional cycle from training through evaluation,
deployment, and safety, with re-training closing the loop. The dashed
diagonal highlights the survey's central claim that the cycle is not
purely sequential: training choices shape deployment feasibility,
and the consequences of evaluation, deployment, and safety choices
feed back through re-training.}
\label{fig:lifecycle}
\end{figure}

To make the cross-stage couplings concrete,
Table~\ref{tab:coupling_evidence} records seven examples where a
training, evaluation, or deployment choice reported in the literature is
plausibly connected to an effect in a downstream stage. Some rows
report a direct empirical observation from the cited paper; others
represent cross-paper synthesis or a design implication highlighted in
this survey.

\begin{table*}[t]
\caption{Literature-grounded cross-stage couplings synthesized in this
survey. Each row links a specific upstream decision to a downstream
implication, illustrating that the four lifecycle stages are not
independent. The final column mixes direct empirical observations from
individual papers with cross-paper synthesis; interpretive rows should
not be read as controlled causal experiments.}
\label{tab:coupling_evidence}
\centering
\footnotesize
\begin{tabularx}{\textwidth}{p{0.13\textwidth}XXX}
\toprule
\textbf{Upstream stage} & \textbf{Decision / observation} &
\textbf{Downstream stage affected} & \textbf{Downstream implication} \\
\midrule
Training &
Whisper-style weakly supervised pretraining emphasizes transcription
quality \citep{radford2023robust}. &
Evaluation &
Benchmarks inherit ASR-centric metrics; paralinguistic and prosodic
validity are under-measured (Section~\ref{subsec:eval_coverage}). \\
\midrule
Training &
Adapter / Q-Former bridge between speech encoder and language model
\citep{tang2023salmonn,fan2025alignformer}. &
Deployment &
Bridge layer adds latency; full-duplex stacks must redesign streaming
boundary (Section~\ref{sec:deployment}). \\
\midrule
Evaluation &
Multilinguality coverage is fragmented across incompatible metrics
\citep{liu2025seallmsaudio,papi2025hearing2translate}. &
Deployment &
Practitioners cannot rank multilingual systems for production without
re-running on a target language slice (Section~\ref{sec:deployment}). \\
\midrule
Evaluation &
LLM-as-judge protocols underlie open-ended scoring with only moderate
human correlation \citep{ao2024sdeval,zheng2023judging}. &
Safety &
Judge-based reward signals can miss unsafe prosody or jailbreaks
triggered by acoustic content (Section~\ref{sec:safety_privacy_governance}). \\
\midrule
Deployment &
Quantization and KV-cache pruning are applied for edge feasibility
\citep{xie2024miniomni}. &
Safety &
Downstream safety classifiers and watermark detectors can degrade
under aggressive compression
(Section~\ref{sec:safety_privacy_governance}). \\
\midrule
Training &
Text-only safety alignment of the LLM backbone without audio-side
retraining \citep{yang2024audioachillesheelred}. &
Safety &
Adversarial audio jailbreaks bypass alignment; text-aligned refusal
boundaries do not transfer to speech representations
(Section~\ref{sec:safety_privacy_governance}). \\
\midrule
Deployment &
Streaming inference with speculative decoding and KV-cache eviction
under real-time time-to-first-byte budgets
\citep{kamahori2026voxserve,wei2025specasr}. &
Evaluation &
Static offline benchmark scores fail to reveal streaming-mode accuracy
drop; reported throughput varies by an order of magnitude in conditions
no current benchmark captures (Section~\ref{subsec:eval_coverage}). \\
\bottomrule
\end{tabularx}
\end{table*}
\FloatBarrier